services:
  saasbackend:
    image: javimosch/superbackend:latest
    command: npm run dev
    ports:
      - "${SAASBACKEND_PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules

networks:
  coolify-shared:
    external: true
