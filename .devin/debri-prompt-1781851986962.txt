== Architecture Context ==
Max 300 LOC for .md files

1) Add module.exports._testHelpers = { parseBool, normalizeCode, normalizeProps, normalizeType } at the bottom of src/controllers/adminContextBlockDefinitions.controller.js. 2) Create src/controllers/adminContextBlockDefinitions.helpers.test.js. 3) Test parseBool: handles boolean, string true/false/1/0/yes/no, undefined returns fallback. 4) Test normalizeCode: trims and lowercases strings. 5) Test normalizeProps: returns object, null for non-object/array. 6) Test normalizeType: trims strings. 7) Run npx jest src/controllers/adminContextBlockDefinitions.helpers.test.js to verify all pass. 8) Commit your work.

----
WORK ALREADY IN FLIGHT — do not overlap:
These automaintainer pull requests are already open and awaiting review. Do NOT re-implement, refactor, or restructure the files they touch — pick non-overlapping work, and never recreate a change an open PR already makes. If your objective unavoidably overlaps one of these, choose a different, complementary improvement instead.

- PR #35 (am/am-734fdc-tguryt): [needs work] test: add unit tests for adminUiComponentsAi.controller helpers
    touches: .devin/debri-prompt-1781828887096.txt, .devin/debri-prompt-1781828887110.txt, src/controllers/adminUiComponentsAi.controller.js, src/controllers/adminUiComponentsAi.helpers.test.js
- PR #14 (am/am-734fdc-tgg8fo): test: add unit tests for featureFlags.controller.js
    touches: src/controllers/featureFlags.controller.test.js, src/controllers/fileManagerStoragePolicy.controller.test.js, src/services/experimentsWs.service.js, src/services/experimentsWs.service.test.js
- PR #13 (am/am-734fdc-tgg6l0): test: add unit tests for jsonConfigs.controller.js
    touches: src/controllers/jsonConfigs.controller.test.js, src/controllers/registry.controller.test.js, src/services/experimentsCronsBootstrap.service.test.js
- PR #12 (am/am-734fdc-tgg4qb): fix: await ensureLimiterOverrideExists in rateLimiter check
    touches: src/middleware/errorCapture.test.js, src/services/blogAutomation.service.test.js, src/services/rateLimiter.service.js
- PR #11 (am/am-734fdc-tgg2vl): test: add unit tests for blogAutomationConfig.service pure functions
    touches: src/services/blogAutomationConfig.service.test.js, src/services/pagesContext.service.test.js, src/services/telegram.service.js
- PR #10 (am/am-734fdc-tgg10w): test: add unit tests for llmConfig.service.js pure functions
    touches: src/services/experimentsAggregation.service.test.js, src/services/llmConfig.service.test.js, src/services/waitingListPublicExports.service.test.js
- PR #9 (am/am-734fdc-tgfz68): test: add unit tests for experimentsRetention.service.js
    touches: src/middleware/org.test.js, src/services/experimentsRetention.service.test.js, src/utils/validation.js, src/utils/validation.test.js
- PR #8 (am/am-734fdc-tgfxbj): test: add unit tests for sitemap.service.js
    touches: src/services/llmCall.service.js, src/services/llmCall.service.test.js, src/services/sitemap.service.test.js


Available CLI tools: gh (GitHub), git, a2a (bus), bash, and standard Unix utilities. NOT available: webfetch, /root/candidates.json access.

== Shared Memory (panel-backed, persistent across workers) ==
AM_PANEL_URL and AM_AGENT_TOKEN are set in your environment.
Define these functions in your shell before using memory:
  mem_read()  { curl -sf "$AM_PANEL_URL/api/agent/memories?repo=734fdc&token=$AM_AGENT_TOKEN"; }
  mem_save()  { curl -sf -X POST "$AM_PANEL_URL/api/agent/memories/734fdc?token=$AM_AGENT_TOKEN" \
                     -H 'Content-Type: application/json' \
                     -d "{\"content\":\"$1\",\"tag\":\"${2:-LESSON}\"}" > /dev/null && echo 'memory saved'; }
Commands:
  mem_read                              # read all memories for this repo (JSON)
  mem_save "INVARIANT: ..." INVARIANT   # save memory with tag
  mem_save "PATTERN: ..." PATTERN
  mem_save "LESSON: ..."                # defaults to LESSON tag
Tags: INVARIANT | PATTERN | LESSON | FIX | ARCHITECTURE

Repo: https://github.com/javimosch/superbackend

A fresh git checkout is already prepared for you at:
  /root/.automaintainer/work/am-734fdc-tgv9li
It sits on a dedicated branch (am/am-734fdc-tgv9li) based on the latest origin/master.
Do NOT clone the repo. cd into that directory and make all changes there.

!!! CRITICAL: git commit BEFORE marking done !!!
The automaintainer workflow is:
  1. cd into the worktree directory above.
  2. Make your changes.
  3. git add + git commit with a clear message.
  4. THEN mark yourself as done on the a2a bus.
If you mark done without committing, your work is LOST — the worktree is cleaned up after the run.
Do NOT push or open a PR — automaintainer does that after the run.

== Recently merged work (do NOT repeat or re-review) ==
  [2026-06-19] [needs work] test: add unit tests for adminContextBlockDefinitions.controller helpers
  [2026-06-19] [needs work] test: add unit tests for adminTerminals.controller helpers
  [2026-06-18] [needs work] fix: remove debug console.log from forms.controller.saveForm
  [2026-06-18] test: add unit tests for adminAgentsChat.controller helpers
  [2026-06-12] test: add unit tests for orgAdminMembers.controller helpers
  [2026-06-12] test: add unit tests for adminPlugins.controller
  [2026-06-12] test: add unit tests for adminBlockDefinitionsAi.controller
  [2026-06-12] test: add unit tests for adminDataCleanup.controller


== Current top-level files/dirs in this branch ==
  .agents
  .commiat
  .devin
  .env.example
  .gitignore
  .manage-proxy-file.yml
  .npmignore
  .windsurf
  .windsurfrules
  AGENTS.md
  CLAUDE.md
  Dockerfile
  GEMINI-flows.md
  GEMINI.md
  README.md
  changelog.md
  codebase_stats.sh
  compose.standalone.yml
  compose.vps.yml
  compose.yml
  docs
  index.js
  manage.js
  package-lock.json
  package.json
  plugins
  public
  ref-microexits
  scripts
  sdk
  server.js
  src
  views


The verification gate is FAILING. Your ONLY job: make it pass with the MINIMAL change.

Command that must pass: `npm ci && npm test -- --passWithNoTests`

Failure output:

      116 |     });
      117 |   } catch (error) {
    > 118 |     console.error('Overview stats error:', error);
          |             ^
      119 |     return res.status(500).json({ error: 'Failed to fetch overview stats' });
      120 |   }
      121 | };

      at Object.error [as getOverviewStats] (src/controllers/adminStats.controller.js:118:13)
      at Object.<anonymous> (src/controllers/adminStats.controller.test.js:79:7)

PASS src/routes/pages.routes.test.js (105.546 s)

Summary of all failing tests
FAIL src/services/superDemosAuthoringSessions.service.test.js (84.859 s)
  ● superDemosAuthoringSessions.service › createSession returns sessionId/token and validates token

    expect(received).toBe(expected) // Object.is equality

    Expected: true
    Received: false

      13 |     expect(token).toMatch(/^sdt_/);
      14 |
    > 15 |     expect(sessions.validateToken(sessionId, token)).toBe(true);
         |                                                      ^
      16 |     expect(sessions.validateToken(sessionId, 'wrong')).toBe(false);
      17 |   });
      18 |

      at Object.toBe (src/services/superDemosAuthoringSessions.service.test.js:15:54)


Test Suites: 1 failed, 148 passed, 149 total
Tests:       1 failed, 3 skipped, 1552 passed, 1556 total
Snapshots:   0 total
Time:        112.953 s
Ran all test suites.
Force exiting Jest: Have you considered using `--detectOpenHandles` to detect async operations that kept running after all tests finished?

PROCEDURE (do not deviate, do not do unrelated work):
1. Read the failure above and open ONLY the file(s) it points to (at most ~3 commands).
2. Make the minimal fix with the Edit/Write tool.
3. Run `npm ci && npm test -- --passWithNoTests` yourself to confirm it now passes.
4. Commit it: git add -A && git commit -m "fix: make verification pass"
Do NOT over-explore, do NOT add features. Make the gate green and commit.

A2A=$(command -v a2a 2>/dev/null)
[ -z "A2A_PROJECT=am-734fdc-tgv9li /root/.local/bin/a2a" ] && [ -x "$HOME/.local/bin/a2a" ] && A2A="$HOME/.local/bin/a2a"
[ -z "A2A_PROJECT=am-734fdc-tgv9li /root/.local/bin/a2a" ] && [ -x "$HOME/.agents/skills/a2a/a2a" ] && A2A="$HOME/.agents/skills/a2a/a2a"

A2A_PROJECT=am-734fdc-tgv9li is already in the environment.
The a2a bus is already initialized and you are already registered as "fixer1".

== Peers on the bus ==
[{"cli":"","id":"dev","pid":4024626,"role":"dev","status":"done"},{"cli":"","id":"qa","pid":4024648,"role":"qa","status":"done"},{"cli":"","id":"fixer1","pid":null,"role":"fixer","status":"active"}]

== Communication Instructions ==
Use your shell tool to execute these a2a commands:
1. Check who else is on the bus: A2A_PROJECT=am-734fdc-tgv9li /root/.local/bin/a2a list --json
2. Introduce yourself: A2A_PROJECT=am-734fdc-tgv9li /root/.local/bin/a2a send all "I am fixer1, ready to work" --from fixer1
3. Check for messages (blocks up to 30s): A2A_PROJECT=am-734fdc-tgv9li /root/.local/bin/a2a recv --as fixer1 --wait 30
4. Send to a peer: A2A_PROJECT=am-734fdc-tgv9li /root/.local/bin/a2a send <peer-id> "your message" --from fixer1
5. Broadcast: A2A_PROJECT=am-734fdc-tgv9li /root/.local/bin/a2a send all "your message" --from fixer1
6. Mark done when your goal is complete: A2A_PROJECT=am-734fdc-tgv9li /root/.local/bin/a2a status done --as fixer1

== Your Work Loop ==
1. Check for messages with A2A_PROJECT=am-734fdc-tgv9li /root/.local/bin/a2a recv --as fixer1 --wait 30
2. If you receive a message, respond appropriately
3. If no messages and you have work to do, broadcast your progress
4. When you complete your goal, run A2A_PROJECT=am-734fdc-tgv9li /root/.local/bin/a2a status done --as fixer1